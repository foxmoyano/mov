# Configuración

- Frontend: URL base de API por entorno.
- Backend: `HOST`, `PORT`, `DATABASE_URL`.
- MinIO: endpoint, access key, secret key, región y bucket.
- Observabilidad: `RUST_LOG`.

No documentar valores secretos.
