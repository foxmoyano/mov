# Despliegue

Orden conceptual: PostgreSQL/MinIO → secretos/configuración → backend → frontend → observabilidad. La plataforma, TLS, DNS, healthcheck y rollback concreto están pendientes.

El agente no construye imágenes ni ejecuta despliegues.
