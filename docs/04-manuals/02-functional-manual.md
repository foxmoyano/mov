# Manual funcional

El frontend consume listado paginado. El backend filtra título y obtiene detalle desde PostgreSQL, complementándolo con URLs temporales de MinIO. Crear, actualizar, eliminar y exportar no están soportados por el backend.
